rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Card attachments — only the owning user can read/write their attachments
    match /attachments/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
